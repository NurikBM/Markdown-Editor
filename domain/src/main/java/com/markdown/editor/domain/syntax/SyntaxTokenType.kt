package com.markdown.editor.domain.syntax

/**
 * Enumerates the semantic categories for code syntax tokens.
 */
enum class SyntaxTokenType {
    KEYWORD,
    TYPE,
    STRING_LITERAL,
    NUMBER_LITERAL,
    COMMENT,
    FUNCTION,
    PUNCTUATION,
    PLAIN
}

